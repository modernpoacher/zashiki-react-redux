export default function getStageProps ({ state, resource, definition, response, errors, gears, onChange, onSubmit }) {
  return {
    state,
    resource,
    definition,
    response,
    errors,
    gears,
    onChange,
    onSubmit
  }
}
